import { useState } from 'react';import { toast } from 'sonner';import { api } from '../api/client';import { Friend, FriendRequest, Notification, TripInvitation, User } from '../types';import { EmptyState, FriendCard, FriendRequestCard, LoadingSkeleton, NotificationCard, TripInvitationCard, UserCard } from '../components/Cards';import { useInfinite } from '../hooks/useInfinite';
function Shell({children}:{children:React.ReactNode}){return <main className="mx-auto max-w-5xl space-y-5 p-4"><nav className="glass flex flex-wrap gap-3 rounded-3xl p-4"><a href="/search">Search Users</a><a href="/friends">Friends</a><a href="/requests">Friend Requests</a><a href="/invitations">Trip Invitations</a><a href="/notifications">Notifications</a></nav>{children}</main>}
export function SearchUsers(){const [q,setQ]=useState('');const {items,loading}=useInfinite<User>(`/friends/search/?search=${encodeURIComponent(q)}`);async function add(id:string){await api('/friends/requests/send/',{method:'POST',body:JSON.stringify({receiver_id:id})});toast.success('Friend request sent')}return <Shell><input className="glass w-full rounded-2xl p-4" placeholder="Search by username, name, or exact email" value={q} onChange={e=>setQ(e.target.value)}/>{items.map(u=><UserCard key={u.id} user={u} action={<button className="btn bg-indigo-600 text-white" disabled={u.friend_status==='pending'||u.friend_status==='accepted'} onClick={()=>add(u.id)}>{u.friend_status==='pending'?'Pending':'Add Friend'}</button>}/>) }{loading&&<LoadingSkeleton/>}</Shell>}
export function Friends(){const {items}=useInfinite<Friend>('/friends/');return <Shell>{items.length?items.map(f=><FriendCard key={f.id} friend={f} onInvite={()=>toast.info('Select a trip from trip detail to invite friends')} onRemove={async()=>{await api(`/friends/${f.id}/remove/`,{method:'DELETE'});toast.success('Friend removed')}}/>):<EmptyState title="No friends yet" body="Search users by username to grow your network."/>}</Shell>}
export function FriendRequests(){const {items}=useInfinite<FriendRequest>('/friends/requests/?status=pending');return <Shell>{items.map(r=><FriendRequestCard key={r.id} request={r} onAccept={async()=>{await api(`/friends/requests/${r.id}/accept/`,{method:'POST'});toast.success('Accepted')}} onReject={async()=>{await api(`/friends/requests/${r.id}/reject/`,{method:'POST'});toast.success('Rejected')}} onCancel={async()=>{await api(`/friends/requests/${r.id}/cancel/`,{method:'POST'});toast.success('Cancelled')}}/> )}</Shell>}
export function TripInvitations(){const {items}=useInfinite<TripInvitation>('/trips/invitations/');return <Shell>{items.map(i=><TripInvitationCard key={i.id} invitation={i} onAccept={async()=>{await api(`/trips/invitations/${i.id}/accept/`,{method:'POST'});toast.success('Joined trip')}} onReject={async()=>{await api(`/trips/invitations/${i.id}/reject/`,{method:'POST'});toast.success('Declined')}}/> )}</Shell>}
export function Notifications(){const {items}=useInfinite<Notification>('/notifications/');return <Shell><button className="btn bg-indigo-600 text-white" onClick={()=>api('/notifications/mark_all_read/',{method:'POST'})}>Mark all read</button>{items.map(n=><NotificationCard key={n.id} notification={n} onRead={()=>api(`/notifications/${n.id}/mark_read/`,{method:'POST'})} onDelete={()=>api(`/notifications/${n.id}/`,{method:'DELETE'})}/>)}</Shell>}
