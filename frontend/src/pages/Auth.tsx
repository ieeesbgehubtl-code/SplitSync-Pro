import { useForm } from 'react-hook-form';import { z } from 'zod';import { zodResolver } from '@hookform/resolvers/zod';import { http } from '../api/http';import toast from 'react-hot-toast';
const loginSchema=z.object({username:z.string().min(1),password:z.string().min(8)});const registerSchema=loginSchema.extend({email:z.string().email(),full_name:z.string().min(2)});
export function Login(){const{register,handleSubmit}=useForm({resolver:zodResolver(loginSchema)});return <main className="grid min-h-screen place-items-center p-4"><form className="glass w-full max-w-md rounded-3xl p-8" onSubmit={handleSubmit(async(v)=>{const r=await http.post('/auth/login/',v);localStorage.setItem('access',r.data.access);localStorage.setItem('refresh',r.data.refresh);toast.success('Welcome back');location.href='/dashboard';})}><h1 className="mb-6 text-3xl font-black">Login</h1><input className="mb-3 w-full rounded-xl p-3" placeholder="Username" {...register('username')}/><input className="mb-3 w-full rounded-xl p-3" type="password" placeholder="Password" {...register('password')}/><button className="btn w-full bg-indigo-600 text-white">Login</button><a className="mt-4 block text-center" href="/register">Create account</a></form></main>}
export function Register(){const{register,handleSubmit}=useForm({resolver:zodResolver(registerSchema)});return <main className="grid min-h-screen place-items-center p-4"><form className="glass w-full max-w-md rounded-3xl p-8" onSubmit={handleSubmit(async(v)=>{await http.post('/auth/register/',v);toast.success('Account created');location.href='/login';})}><h1 className="mb-6 text-3xl font-black">Create account</h1><input className="mb-3 w-full rounded-xl p-3" placeholder="Full name" {...register('full_name')}/><input className="mb-3 w-full rounded-xl p-3" placeholder="Username" {...register('username')}/><input className="mb-3 w-full rounded-xl p-3" placeholder="Email" {...register('email')}/><input className="mb-3 w-full rounded-xl p-3" type="password" placeholder="Password" {...register('password')}/><button className="btn w-full bg-indigo-600 text-white">Register</button></form></main>}
